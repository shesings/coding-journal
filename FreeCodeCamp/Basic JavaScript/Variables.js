// 8 different data types
// undefined, null, boolean, string, symbol, bigint, number, object

// variables = labels = which allow to point TO the data, rather than just using the data itself

// this is just declaring the variable, NOT assigning
var myName;

